const app = {
    // apiTimeout: parseInt(import.meta.env.VITE_API_TIMEOUT || "5000"),
    // defaultLanguage: import.meta.env.VITE_DEFAULT_LANGUAGE || "en-US",
    // maxRetries: parseInt(import.meta.env.VITE_MAX_RETRIES || "3"),
    name: "GoDaddy",
    version: "v1.0.0",
};

export default app;
